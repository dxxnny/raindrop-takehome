SCHEMA >
    `order_id` String,
    `order_item_id` Int32,
    `product_id` String,
    `seller_id` String,
    `shipping_limit_date` DateTime,
    `price` Float64,
    `freight_value` Float64

ENGINE "MergeTree"
ENGINE_SORTING_KEY "order_id, order_item_id"

